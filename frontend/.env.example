# Backend URL for API calls in production
# When deployed on Render, this will be your backend URL, e.g.:
# VITE_API_BASE_URL=https://notes-manager-backend.onrender.com
# Leave empty for development (uses relative /api path through Vite proxy)
VITE_API_BASE_URL=